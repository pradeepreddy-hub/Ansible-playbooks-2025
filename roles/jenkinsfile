pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Build') {
            steps {
                sh '''
                   ansible --version
                   ansible-playbook -i inventory roles/tomcat.yml
                '''
            }
        }
    }
}
